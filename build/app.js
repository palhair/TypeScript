"use strict";
const user = {
    name: 'waer',
    roles: [],
    permission: {
        endDate: new Date
    }
};
const nameUser = user['name'];
const roleNames = 'roles';
let roleNames2 = 'roles';
const roles = ['admin', 'user', 'super-user'];
const dfdf = 'user';
